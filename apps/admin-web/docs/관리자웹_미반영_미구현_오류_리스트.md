# 관리자웹 미반영·미구현·오류 종합 리스트

> 2025.03.07 기준 점검 결과

---

## 1. 미반영 (UI/네비에 있지만 연결 안 됨·누락)

| No | 항목 | 내용 | 조치 |
|----|------|------|------|
| 1 | **폼메일 관리** | `/formmail` 페이지 존재하나 ADMIN_NAV_ITEMS에 미포함 → 사이드바에서 접근 불가 | ✅ `lib/admin-nav.ts`에 폼메일 메뉴 추가 완료 |
| 2 | **DB 분배 중복** | `/db-distribution` 페이지 존재, 네비는 `/requests/distribution` 사용. 둘 다 기능 유사함 | `db-distribution` 제거 또는 네비 통합 결정 |
| 3 | **AGENT_NAV_ITEMS 경로** | "제휴업체 매칭 현황" → `/partner/matching` (agent는 `/agent` 경로) | 의도된 동작(공인중개사도 partner 레이아웃 사용) — 확인 |

---

## 2. 미구현 (실제 동작 없음·스텁)

| No | 항목 | 내용 | 조치 |
|----|------|------|------|
| 1 | **Toss 결제** | `app/api/payments/toss-checkout/route.ts`가 mock-checkout으로 리다이렉트만 함 | Toss Payments API 연동 구현 |
| 2 | **SMS/알림톡 실제 발송** | `lib/api/notifications.ts`의 `sendAlimtalk`, `sendSMS`는 실제 API 호출 없이 로그만 저장 | Aligo·카카오 API 연동 |
| 3 | **설정 > 알림 설정** | `/settings` 알림 체크박스(새 요청/배정완료/출금신청/결제완료) 변경 시 저장 안 됨 | ✅ 마이그레이션 + load/save 구현 완료 |

---

## 3. 잠재적 오류·개선

| No | 항목 | 내용 | 조치 |
|----|------|------|------|
| 1 | **getSupabase 서버 사용** | `lib/api/settings.ts`가 `getSupabase()` 사용 — 서버에서는 `null` 반환 가능 | 클라이언트 전용. settings 페이지는 'use client'이므로 OK |
| 2 | **withErrorHandler 미적용 API** | 일부 API 라우트에 `withErrorHandler` 미적용 | 전체 API 적용 검토 |
| 3 | **환경변수 placeholder** | `lib/supabase.ts` — placeholder 키면 null 반환 | 배포 시 실제 키 설정 필요 |

---

## 4. 빌드·테스트

- ✅ `npm run build` 성공
- ✅ `npm test` 121개 통과
- ⚠️ admin.settlements.test: mock DB error 시 console.error 출력 (의도된 동작)

---

## 5. 우선순위

1. ~~**즉시** — 폼메일 메뉴 추가 (미반영)~~ ✅ 완료
2. ~~**즉시** — 알림 설정 저장/로드 구현 (미구현)~~ ✅ 완료
3. **단기** — Toss 결제 연동 (실서비스)
4. **단기** — SMS/알림톡 실제 발송 (실서비스)

---

## 6. 적용 방법 (알림 설정)

Supabase에 마이그레이션 적용:
```bash
# supabase/migrations/20260307120000_site_settings_notification_prefs.sql
# site_settings.notification_prefs JSONB 컬럼 추가
```
